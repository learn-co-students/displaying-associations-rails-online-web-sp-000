<h1><%= @category.name %></h1>

<!-- Add code here to list a category's posts. -->
<% @category.posts.each do |post| %>
  <%= link_to post.title, post_path(post) %>
  <%= link_to "My Category", category_path(post.category) %>
<% end %>
