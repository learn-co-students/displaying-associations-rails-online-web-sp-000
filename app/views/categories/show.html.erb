<h1><%= @category.name %></h1>

<h3><%= pluralize(@category.posts.count, 'Post') %></h3>
<!-- Add code here to list a category's posts. -->
<ul>
    <% @category.posts.each do |c| %>
        <li><%= link_to c.title, post_path(c) %></li>
    <% end %>
</ul>
