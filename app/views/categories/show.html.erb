<h1><%= @category.name %></h1>


<h3><%= pluralize(@category.posts.count, "Post") %></h3>
<% @category.posts.each do |p| %>
    <p><%= link_to p.title, post_path(p) %></p>
    <p><%= p.description %></p>
<% end %>
